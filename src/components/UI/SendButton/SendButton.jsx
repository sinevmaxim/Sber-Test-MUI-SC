import React from "react";
import Button from "@mui/material/Button";

const SendButton = () => {
  return (
    <Button fullWidth label="Send" type="submit" variant="contained">
      Отправить
    </Button>
  );
};

export default SendButton;
